[target.xtensa-esp32-none-elf]
# Hardware flash runner (default)
runner = "espflash flash --monitor --chip esp32 --flash-freq 40mhz --flash-size 4mb --flash-mode dio --port /dev/tty.usbserial-11103"

# Wokwi simulation runner (use: cargo run --release --features sim)
# runner = "wokwi-server --chip esp32 --elf target/xtensa-esp32-none-elf/release/edge-peripheral"

[build]
rustflags = [
  "-C", "link-arg=-nostartfiles",
]
target = "xtensa-esp32-none-elf"


[env]
ESP_LOG = "info"

# Xtensa only:
# Needed for nightly, until llvm upstream has support for Rust Xtensa.
[unstable]
build-std = ["alloc", "core"]